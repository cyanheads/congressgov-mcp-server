{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "congressgov-mcp-server",
  "version": "0.7.0",
  "description": "Access U.S. congressional data - bills, votes, members, committees - through MCP. STDIO & Streamable HTTP.",
  "author": {
    "name": "cyanheads",
    "email": "casey@caseyjhand.com",
    "url": "https://github.com/cyanheads"
  },
  "homepage": "https://github.com/cyanheads/congressgov-mcp-server#readme",
  "repository": "https://github.com/cyanheads/congressgov-mcp-server",
  "license": "Apache-2.0",
  "keywords": ["mcp", "mcp-server", "model-context-protocol", "congress", "legislation", "bills"],
  "userConfig": {
    "CONGRESS_API_KEY": {
      "type": "string",
      "title": "Congress.gov API key",
      "description": "Optional. Defaults to DEMO_KEY (30 req/hour); your own key from api.data.gov allows 5,000 req/hour.",
      "sensitive": true,
      "default": ""
    }
  },
  "mcpServers": {
    "congressgov-mcp-server": {
      "command": "npx",
      "args": ["-y", "@cyanheads/congressgov-mcp-server"],
      "env": {
        "MCP_TRANSPORT_TYPE": "stdio",
        "CONGRESS_API_KEY": "${user_config.CONGRESS_API_KEY}"
      }
    }
  }
}
