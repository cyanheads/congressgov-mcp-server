---
summary: "congressgov_committee_lookup name filter and resolution now page the full committee set instead of only the first 250; API-key rate-limit docs corrected to 5,000 req/hr; @cyanheads/mcp-ts-core ^0.10.9 → ^0.10.10; bun audit cleared 8 transitive advisories to 0."
security: true
---

# 0.3.32 — 2026-06-30

## Fixed

- **`congressgov_committee_lookup`** — the `list` + `filter` path and the name→code resolution path now page the complete committee set (455 for the House, 818 across all chambers) via `pagination.nextOffset` before matching, instead of searching only the first 250 rows. A committee past row 250 — e.g. the Consumer Affairs and Coinage Subcommittee (`hsba02`, ~403 of 455) — was previously dropped with no notice, so the short result read as "doesn't exist." Both call sites share a new `fetchAllCommittees` helper; worst case ≈4 requests (818 ÷ 250). ([#41](https://github.com/cyanheads/congressgov-mcp-server/issues/41))
- **API-key rate-limit documentation** — corrected from 1,000 to 5,000 req/hr across the config schema `.describe()`, `.env.example`, `server.json`, `manifest.json`, `README.md`, and the agent docs. The in-code limiter and the rate-limit error message were already 5,000; only the docs were stale. ([#42](https://github.com/cyanheads/congressgov-mcp-server/issues/42))

## Security

- **Transitive advisories cleared** — `bun audit` went from 8 advisories (2 high, 6 moderate) to 0. Refreshing the lockfile (`audit:refresh`) re-resolved stale transitive entries to already-patched versions: `vite` `8.0.14` → `8.1.2`, `hono` `4.12.26` → `4.12.27`, `js-yaml` `3.14.2` → `3.15.0`. The advisories came in through build- and test-chain transitives (`vitest`, `depcheck`, `@modelcontextprotocol/sdk`), not through `@cyanheads/mcp-ts-core`'s own code.

## Dependency bumps

- `@cyanheads/mcp-ts-core` `^0.10.9` → `^0.10.10`
- `@biomejs/biome` `^2.5.0` → `^2.5.1`
- `@types/node` `^26.0.0` → `^26.0.1`
