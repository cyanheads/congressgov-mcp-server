---
summary: "mcp-ts-core ^0.9.1 → ^0.9.6, zod added as direct dep, manifest.json + .mcpbignore scaffolded for MCPB bundle support, install badges added to README."
breaking: false
security: false
---

# 0.3.21 — 2026-05-23

## Changed

- **`@cyanheads/mcp-ts-core` `^0.9.1` → `^0.9.6`** — picks up upstream fixes and additions across the minor series.
- **`zod` `^4.4.3` added as direct dep** — was previously a transitive peer; now explicit in `dependencies`.
- **`manifest.json` scaffolded** — MCPB bundle metadata for one-click install in Claude Desktop. Env var: `CONGRESS_API_KEY` (optional). `bun run bundle` produces `dist/congressgov-mcp-server.mcpb`.
- **`.mcpbignore` added** — excludes `node_modules/`, `src/`, scripts, and dev files from the bundle artifact.
- **Install badges added to README** — Claude Desktop (`.mcpb`), Cursor, and VS Code deep-link badges.
